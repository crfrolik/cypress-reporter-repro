# cypress-repro

1. `yarn install` or `npm install`
2. `yarn cypress run` or `./node_modules/cypress/bin/cypress run`

Passing tests get a valid `id` in the reporter.

Failed tests get no `id` (`undefined`)

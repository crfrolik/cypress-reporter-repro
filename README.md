# cypress-repro

1. `yarn install` or `npm install`
2. `yarn cypress run` or `./node_modules/cypress/bin/cypress run`

Notice that the screenshot has a height of 1px, when it should have a height of 200px.
